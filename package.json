{
  "name": "@unumid/id-verification-types",
  "version": "0.0.10",
  "description": "shared typescript types for identity verification provides that unum id uses: currently HyperVerge and Prove",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@unumid/types": "git+ssh://git@github.com:UnumID/types.git#3.51.1",
    "typescript": "^4.7.4"
  }
}
